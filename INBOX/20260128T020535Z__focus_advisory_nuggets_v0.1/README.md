# focus_advisory_nuggets_v0.1

PUBLIC-SAFE advisory nuggets to help CoPrime sprint CoStacks without goalpost drift.

Include (suggested):
- WIP cap: 1-2 active CoStacks streams; everything else PARKED w/ pointer
- Trunk-based / small-batch integration
- LLM use: planner/critic only; execution in PS7 DO blocks; verifiers fail-closed
- Stop-rule: explicit CoStacks MVP acceptance criteria (freeze scope until PASS)
- Ask CoPrime for canonical MasterPlan URL_RAW + insertion anchors (do not guess)

(Do NOT place IP-sensitive details here.)
